# 自己進化チューニングループ (較正格子) 設計

作成: 2026-08-21。LLM チューニングの自動化検討 (retrieval few-shot / 正解ラベル整備 /
自己進化ループの議論) の帰結をまとめた全体設計。**実装前の合意用文書** — 着手順・
コンポーネント境界・不変条件をここで固定する。

---

## 1. 目的と、自動化しないものの宣言

**目的**: プロンプト (rubric) チューニングの「測る → 例を良くする → 変更を試す → 戻す」
のループから人手作業を可能な限り除き、人間の関与を以下の 2 点に濃縮する:

1. **目的の定義** (何が high か・PIR — 真偽でなく準拠点の問題)
2. **fail-closed の拒否権** (証拠付き提案の承認 / パネルすら割れる係争の裁定)

**自動化しない理由が原理的なもの (3 つの壁)**:

- **壁 1 — 相関した盲点**: 全 LLM は重なった学習分布を共有し、新奇事象・ローカル文脈・
  稀少クラスで誤りが相関する。多様パネルは壁を細らせるが消せない。かつ本ツールの
  致命的失敗 (稀少 high の取りこぼし) はまさにこの領域にある。
- **壁 2 — 規準の問題**: 重要性の定義はドクトリンの事実であり、投票では到達できない。
- **壁 3 — 合意 Goodhart**: パネル合意率を最適化目標にすると、システムは「モデル間
  コンセンサス = Web の平均事前分布」へドリフトする。合意は routing にのみ使い、
  **報酬 (最適化の目標関数) には決して使わない**。

**人間も oracle ではない** (バッチ承認事故 07-26、測定誤りの実績)。人間を残す根拠は
正確性ではなく: ①誤りの脱相関 (Web コーパス外の判定器) ②帰結の非対称 (veto の誤りは
回復可能、auto-apply の誤りは複利) ③責任の非委譲性。**人間の裁定も較正対象とする**
(遅延正解による事後監査)。

## 2. 設計原則 (既存原則との接続)

| 原則 | 本設計での適用 |
|---|---|
| 指示より決定論的関門 | 適用 (apply) は人承認のみ・rollback だけ無人、を code で強制 |
| 承認キュー 3 原則 (起票→レビュー UI→承認=適用の終端保証) | 提案・係争の両キューに適用 |
| 成功も記録する | パネル裁定・人間裁定・自動採用のすべてに来歴と結果を永続化 |
| 対照群なしでは何も言えない | 全評価は 基準1/基準2(対照)/変種 の 3 者比較 (gold set 既設) |
| 母集団の反転 > フィルタ追加 | ラベルは「LLM 出力の採点」でなく「外部正解との突合」を起点に定義 |
| ICD 203 (確度の規律) | 運用者自身の裁定にも適用 (override の事後成績を可視化) |

## 3. 全体アーキテクチャ: 較正格子

正しさはどの単一ノードにも宿らない。強い順に並ぶ「すべて誤りうる証拠源」が、
重なる領域で相互較正する格子として全体を構成する。

```
[E0 決定論的事実]  feed 帰属 (犯行声明)・引用実在チェック・schema/辞書ゲート
      │  (最強の錨。ただし無謬ではない — E1 突合の実測ノイズ 9% §10.2b、
      │   突合キーの LLM 依存 §13-1。錨も監査対象)
[E1 遅延正解]      後日の MITRE 帰属確定・dedup クラスタでの続報整合・
      │            PIR 事後マッチ・訂正報道 — 「未来が過去の判定を裁く」
[E2 多様パネル]    異系統モデル群 + 条件付け (盲検再判定・摂動安定・含意分解)
      │            E0/E1 と重なる層で 合意度×実正解 の較正曲線を常時測定
[E3 人間裁定]      パネル分裂のみ着弾。裁定は E1 で事後監査 (人間も較正される)
[E4 ドクトリン]    PIR・rubric の意図。機械は「誤り」を問えないが
                   「内部不整合」「現実との乖離」(PIR 逆転検出の実例) は指摘する
```

**確信度ルーティング** (格子の出力):
- 高確信帯 (較正曲線で実測された帯) → 自動採用 → ラベル化・few-shot プール供給
- パネル分裂・低確信 → E3 (人間キュー) へ
- E1 未到着で確定不能 → 「係争中」として保持 (即断しない)

## 4. コンポーネント設計

### C1: 遅延正解の収穫 (格子の燃料) — 最初に作る
- 週次ジョブ。「当時の判定 vs 後日確定した事実」を突合し `tuning_labels` に書く。
- 初期ソース: ①feed 帰属×ニュース記事 (victim_org ±5 日突合 — 実証済み手法) ②MITRE
  週次同期での帰属確定 ③dedup クラスタ内の判定不整合 ④PIR 事後マッチと importance の乖離。
- テーブル: `tuning_labels(id, article_id, field, label_value, source(E0..E3), strength,
  arrived_at, provenance, superseded_by)`。**新列 3 点セット規約に従い、消費者 (C3/C8) を
  同時に実装**する。

### C2: 多様パネル裁定
- 陪審: Gemma 26B (現用) + 別系統の異議者 (実装は gpt-oss:20b — 当初案 Llama 8B は
  未 pull かつ別ファミリの方が脱相関に効くため変更、§10.2b) + 外部 LLM (分裂時の
  上級監査、ティア明示割当 + 予算 cap)。§4 denylist 準拠はモデルティア既存 3 層防御に乗る。
- 条件付け: 盲検 (元判定を見せず証拠のみ) / 摂動再判定 (言い換え安定性) / 含意分解
  (根拠引用の実在は E0 チェック、引用→主張の含意のみ問う)。
- **呼び出し点は 2 つだけ**: ①raw 不一致・係争の裁定 ②rubric 変種の評価。
  per-article には使わない (コスト不変条件 §6)。

### C3: 較正層
- E0/E1 が存在する層で「パネル合意パターン × 実正解率」の較正曲線を層別に維持。
- 外挿 (正解のない層への適用) は**明示された仮定**として文書化し、ドリフト番兵
  (合意率分布の変化検知) を常設監査に登録する。

### C4: 確信度ルーティング (§3 のとおり)

### C5: 人間裁定キュー + 人間の較正
- 着弾はパネル分裂のみ (全不一致の 1 桁減を見込む)。1 日 5 分 cap。
- 裁定環境の設計自体が精度変数: 証拠の提示・2 択化・バッチ承認の抑止 (07-26 教訓)。
- 人間裁定も E1 到着時に事後監査し、override 成績を可視化する。

### C6: 提案生成 + 自動評価
- LLM が不一致サンプルから rubric 改善案を起案 → gold set 3 者比較を自動実行 →
  「効果 X (揺らぎ床 ±Y)・回帰面ゼロ」の証拠付き提案としてキューへ。
- **適用は人承認のみ** (版履歴付き block 編集として — 層分け基盤に乗る)。

### C7: 自動 rollback 関門 (唯一の無人適用)
- cutover 後の週次指標が劣化したら該当プロンプトを前版へ自動復帰 + ops 通知。
- fail-closed 方向のみ無人、が本設計で唯一の自動適用。
- **無人で作動するのは破局ゲートの FAIL のみ** (2026-08-22 改訂、独立レビュー第 2 回
  H6): 分布シフト >20pt (判定傾向の変質) と全滅級低下 >15pt に限定。充足率の小幅
  ゲート (1-2pt) を無人接続すると「下がる時だけ罰する」片側 Goodhart ラチェットになり
  (不変条件 2/10 の実装違反)、要約長 ±15% ゲートはブレビティ改善 (本プロジェクト最大の
  成功パターン) を人間不在で巻き戻す。非破局の FAIL は人間向け週次報告に残る。
  判定 SSoT は verify_prompt_cutover の「破局判定:」行。

### C8: few-shot プール管理 (retrieval few-shot の実体)
- 高確信ラベル付き事例を embedding 近傍検索 (dedup 用 arctic-embed2 基盤を流用) で
  k=2-3 件、composer の例示層へ code 所有の動的注入。
- 例に **rubric 版数を紐づけ**、版を跨いだら無効化 (旧基準を教えない)。
- 例プールにも層化を適用 (稀少クラスの偏り防止)。

## 5. ガバナンス不変条件 (違反 = 設計違反)

1. パネル合意は routing 専用。**最適化の目標関数に使わない** (壁 3)。
2. ループの報酬・合否判定は E0/E1 層のみに錨を打つ。
3. 前向き適用 (rubric 変更) は人承認のみ。無人適用は rollback (後ろ向き) だけ。
4. すべての裁定 (自動・パネル・人間) に来歴を残し、E1 で事後監査可能にする。
5. 評価は常に 3 者比較 (対照群必須)。
6. per-article のパネル呼び出し禁止 (LLM 時間予算の保護)。
7. モデル選定は §4 denylist / ティア 3 層防御に乗る。外部 LLM は明示割当 + 予算 cap。
8. **人間は加速材であって燃料ではない** (2026-08-21 追加、§12): すべての人間タッチ
   ポイントは無応答時の既定挙動 (保守側) と TTL を持ち、人間の非介在がループの進行を
   停止させない。事前の人承認を要求するのは前向き rubric 適用 (不変条件 3) のみで、
   それ以外の人間関与は事後 veto (非同期の拒否権) として設計する。
9. キューは必ず (a) TTL (b) 上限件数 (c) superseded 自動失効 を持つ。溜まり続ける
   キューを作らない (07-31 承認キュー全数調査で「気付かれず滞留」を実証済み)。
10. **充足率の上昇単独を「改善」と判定しない** (2026-08-22 追加、§13-4): 正解参照の
    ない自動ゲートは充足率 Goodhart に無防備であり、rubric 変更・自動提案 (C6) の合格
    条件は「劣化なし」+「正解参照可能なフィールドでの非悪化」で構成する。埋まる方向の
    変化は正解が無い限り「差」であって「改善」ではない (§10.2d の認識をゲートに反映)。
11. **TTL・自動失効は非破壊** (2026-08-22 追加、§13-2): 無応答の既定処理はキュー状態の
    整理にとどめ、E0/E1 ラベル (錨) を消さない。錨を無効化できるのは人間裁定 (E3) のみ。
12. **足切りの自動化は死角を作らない** (2026-08-22 追加、§13-7): 棄却された記事は全
    ゲート (goldset / cutover verify / fill-rate 監査) から見えないため、ヘッド・生徒に
    よる配信前足切りは必ずシャドー記録 (LLM と不一致の足切り候補の永続化) から始める。
    足切りの cutover には、足切られた母集団への固定サンプル再判定 (メイン LLM で週 N 件)
    の同時実装を義務化する — 番兵なしの足切り cutover は設計違反。
13. **パリティ・優越の主張はラベルのある場のみ、かつ検出力のある検定でのみ**
    (2026-08-22 追加 / 同日 §10.2e の失敗を受けて後段を追記): E1 正解の無いフィールドで
    主張できるのは「メインとのパリティ (不一致率が床内)」まで。優越の主張と、それを
    根拠にした cutover は、正解参照可能なフィールドでの実証がある場合のみ許す。
    ラベル無し場の「差」は改善でも劣化でもなく「差」である (不変条件 10 と対)。
    **さらに「ラベルがある」だけでは足りない — 課題が退化していないこと (選択肢が
    実在する・床が入力摂動で作られている) を確認してから主張する。検出力ゼロの検定に
    基づく「棄却」は「採用」と同じ誤りであり、決定記録に書いてはならない** (§10.2e)。
14. **遠隔監督ラベルは突合の両辺が独立ソースであることを構造的に保証する**
    (2026-08-22 追加、§10.2e / 独立レビュー H1): 同一ソース・同一収集器に由来する
    突合はラベルとして数えない。E1 錨の 84% が単一ソースの自己突合だった実測を受けた
    条項で、これを満たさないラベルの上に建てた測定は「遅延正解」を名乗れない。

## 6. 段階的実装計画

| Phase | 内容 | 完了条件 |
|---|---|---|
| P1 | C1 遅延正解収穫 (①feed 突合 ②MITRE 確定 — §10.3) + 既存レビューの E3 producer 合流 (§11-A) + tuning_labels + 消費者としての簡易表示 | 週次でラベルが自動蓄積し、種別ごとの件数が UI で見える。人間が一切触らなくてもラベルが増え続ける (§12) |
| P2 | C6 後半 (gold set 3 者比較の cron 化) + C7 自動 rollback | rubric 変更の翌週に自動で合否が出て、劣化時に無人で戻る |
| P3 | C2 パネル + C3 較正曲線 (E0/E1 層で測定開始) | 合意度×正解率の曲線が層別に出る |
| P4 | C4 ルーティング + C5 人間キュー (裁定 UI) | 不一致の大半が自動裁定され、人にはパネル分裂のみ届く |
| P5 | C8 few-shot 注入 (judgment から) + C6 前半 (提案生成) | 3 者比較で few-shot の効果が測定され、証拠付き提案が届く |

各 Phase は独立にデプロイ可能・独立に rollback 可能とする。P1-P2 は既存部品の組合せで
小さく、P3 以降が新規性の中心。

## 7. リスクと未解決問題

- **外挿仮定の検証不能性**: 較正曲線の「正解のない層への転移」は原理的に検証できない。
  緩和はドリフト番兵と、稀少クラスを常に E3 (人間) 側へ倒す保守ルーティングのみ。
- **パネルのコスト/時間**: 分裂率が想定より高いと E3 が溢れる。P3 で実測してから
  P4 の閾値を決める (単発見積りを本番に外挿しない)。
- **ドクトリン変更時の較正リセット**: PIR/rubric の大改訂は較正曲線と few-shot 例の
  前提を壊す。版数紐づけで自動失効させるが、失効直後は自動採用帯が縮む (仕様)。
- **稀少クラスの標本不足**: high・日本標的の較正は標本が集まりにくい。層化と
  「不足層は自動化しない」の明示が必要。

## 8. 着手前に利用者の決定が要る事項

1. **外部 LLM (パネル上級監査) の使用範囲**: 記事本文が外部送信される。ティア明示
   割当の枠内だが、パネル用途への割当可否と月間予算 cap。
2. **裁定 UI の置き場**: 既存レビューキュー群 (承認 UI) への統合で良いか。
3. **P1 の遅延正解ソースの優先順**: 上記 4 種のうちどれから始めるか (推奨: ①feed 突合
   ②MITRE 確定 — いずれも決定論で実装が軽い)。

## 9. 終着点: この設計が向かう先 (2026-08-21 追記)

few-shot は終着点ではなく H1 の技法である。全体は 3 つの地平線で進む:

- **H1 文脈内適応** (few-shot + rubric 編集): 凍結された汎用モデルの操縦。天井は
  ベースモデル能力。移行判定 = gold set 改善が揺らぎ床に沈む (測定された飽和)。
- **H2 重みへの焼き込み** (SFT/自己蒸留): few-shot の上位互換ではなく別エラークラス
  (出力短縮×品質維持・レイテンシ) の手。焼くのは安定した形式規律のみ、ドクトリンは
  プロンプトに残す。参入条件 = few-shot で不動のエラークラスの実測 + ラベル充足。
  **具体構成・文献根拠・RL の位置づけは
  [self_improvement_literature_survey.md](self_improvement_literature_survey.md)
  の統合結論を SSoT とする (2026-08-22)** — 3 段カスケード (古典 ML ヘッド → 小型 dense
  生徒 → 凍結メイン)、学習則は KTO を第一段・RLVR (決定論報酬限定) を第二段。
  パネル・暗黙信号を重み学習の報酬にしないことは §5 不変条件 1 の拡張として不変。
- **H3 モデル可換性 (真の終着点)**: few-shot も SFT もモデル自体も交換可能な「生徒」。
  恒久資産は格子そのもの — 検証済みラベル・較正曲線・gold set・ドクトリン。
  **成功の定義: モデルを乗り換えても、(a) eval が即日で新旧を測定し (b) few-shot
  プールが文脈で再係留し (c) 必要なら同じラベルから再蒸留して、数日で判断品質が
  回復すること。** モデル更新が博打でなく測定可能なアップグレード判断になる。

この定義により、本設計の各コンポーネントの価値序列も定まる: C1 (ラベル) と C3
(較正) が恒久資産、C8 (few-shot) と将来の SFT は資産の消費形態、パネル (C2) は
資産を増やす製造装置である。

## 10. シャドー先行の原則と決定記録 (2026-08-21 追記)

### 10.1 シャドー運用原則 (全 Phase に適用)

**本設計のいかなるコンポーネントも、シャドー期間の計測を経ずに本番へ接続しない。**

- シャドー = 格子 (labels / panel_verdicts / 較正曲線) は**自分のテーブルに書くだけ**で、
  本番パイプラインは一切それを読まない。few-shot 注入・自動 rollback・自動採用ラベルの
  消費はすべて cutover 後にのみ有効化する。
- 各 Phase の完了条件を「実装完了」から「**シャドー計測完了 + cutover 判断**」に改める。
  cutover 判断は計測値 (下記) を利用者に提示して行う。
- 先例に従う: PIR shadow mode (UI のみ・logic 未注入・観察期間で採否) /
  subject-gate 型の env flag 即時 rollback を各接続点に必ず用意する。

### 10.2 外部 LLM (上級監査) の段階的検証

外部 LLM の利用は**条件付き承認** (2026-08-21)。接続は以下の実測を経てから:

1. **P3 シャドーで、まずローカル 2 モデルのみ** (26B + 8B) のパネルを裁定ストリームに
   通し、以下を数週間計測する:
   - 不一致率 (パネルが必要になる母数)
   - **分裂率 (= 外部 LLM にエスカレーションされるはずだった件数/週)**
   - 分裂サンプルのトークン量 (本文送信量の実数)
2. 計測値から**外部呼び出しの月間件数・送信トークン量・概算費用**を算出し、利用者に
   提示 → 予算 cap と接続可否を確定する (単発見積りを外挿しない)。
3. 接続後も cap 超過時はローカルのみへ自動縮退 (fail-closed)。

### 10.2b シャドー計測の初回記録 (2026-08-22、過去 90 日 backfill。同日訂正)

E1 主体ラベル全量 (56 件、body 保持 90 日が上限) でパネルを一括裁定した結果:

| 層 | 件数 | パネル結果 |
|---|---|---|
| 対照 (本番 = E1 正解) | 22 | **22/22 unanimous_correct** |
| 「係争」(本番 ≠ E1 正解) | 3 | 3/3 で パネルは E1 と不一致 (2 件は両者とも空、1 件は両者とも別の値) |
| 候補未到達 (裁定対象外) | 38 | — (構造ゲートの到達率問題として別掲) |

**訂正 (同日)**: 当初この 3 件を「全員一致の誤答 = 壁 1 の実測」と読んだが、記事本文の
突合で **3 件とも E1 ラベル側の誤り** (収穫ノイズ) と確定した — 本番とパネルの判定が
正しかった。ノイズは 2 系統・計 5/56 件 (~9%):

1. **遅延声明の窓誤結合** (3 件): 声明の時刻錨を掲載日にしていたため、発覚 3 か月後に
   掲載された声明が同一組織の**別インシデント**報道と ±5 日で結合した (LexisNexis)。
   記事側の言及は「過去事例への言及」で、主題は攻撃者未公表の新事件 — 空が正解。
   → 錨を発覚日 COALESCE(published_at, created_at) に修正。
2. **ダイジェスト除外の移植漏れ** (2 件): 多数事件まとめに単一主題は無い。eval script に
   あった除外を production 収穫へ移植し忘れた。→ 除外を移植。

**確定した含意**:
- **パネルはラベル監査として機能した** — 「パネル全員一致 × E1 と不一致」が 9% の
  ラベルノイズを 100% 精度で検出した (誤検出ゼロ: 一致 22 件は全て正ラベル)。
  E0/E1 は錨だが無謬ではない (§3 の前提どおり) — この不一致こそ C5 人間裁定の
  正しい着弾物であり、「どちらが誤りか」を人間が裁く (今回は全件ラベル側だった)。
- **真の係争 (ラベル正・判定誤) は 0 件/90 日** — 分裂率も 0%。外部 LLM の
  エスカレーション実量はゼロ近傍で、予算懸念は当面存在しない。
- P4 のトリガは「E0/E1 × パネルの不一致」(決定論) を第一とし、着弾は月 ~1-2 件想定。
  裁定 UI は最小構成でよい。
- ⚠ n が小さい。週次パネルの蓄積で継続再評価する。

### 10.2c C8 few-shot の初回実測 (2026-08-22) — cutover 見送り

judgment への retrieval few-shot 注入 (E1 プール 51 件、embedding 近傍 k=2、自己混入
ガード付き) を goldset 3 者比較 (86 件、床 = 基準 2 実行) で実測:

- **標的 subject_actor_id: 効果なし** (充足 15.1→14.0%、-1.1pt は床 ±3.9pt 内)
- **干渉劣化 2 項目**: event_date **-10.4pt** (床 ±4.3)、compromise_date -2.3pt (床 ±2.0)。
  例示に含む過去記事の文脈が日付系フィールドの抽出を妨害した
- 判断: **JUDGMENT_FEWSHOT は off のまま** (測定利得ゼロ + 回帰面ありの注入はしない —
  08-18/08-21 の「プロンプト行にはコストがある」と同じ判断)。プール・測定 harness・
  版数紐づけは資産として残す (ラベルの質と量が変わったら同じ器で再測定)
- **§9 の H1 移行判定に対する含意**: 「gold set 改善が揺らぎ床に沈む」= H1 (文脈内適応)
  の飽和シグナルの初観測。ただし n=1 手法 (few-shot) の 1 回であり、真の gap は
  候補未到達 26% (構造ゲートの到達率 — few-shot では原理的に解けない)。H2 より先に
  到達率 (feed 経路補完・辞書拡充) が効く

### 10.2d 教師伸びしろ測定 (2026-08-22) — 主要フィールドで教師≈生徒

frontier 教師 (claudecode:sonnet、bridge 経由) で凍結 goldset 86 件を summarizer /
judgment の両ターゲット実行し、現行 gemma と床付き比較 (H2 参入条件 §9 の実測):

- **judgment の核心フィールドは床内**: subject_actor_id 15.1→12.8% (床 ±3.9)、
  一致 92%。主体帰属で教師の優位は観測されず (パネル backfill の「ラベルが在る領域は
  既に正解」と整合)
- 床超えの差分は解釈が割れる: event_date +7.0pt (教師がより多く抽出、一致 59%) /
  named_primary_actor -7.0pt (教師がより保守的 — 収穫汚染の歴史からは美点の可能性)
- summarizer の大差 (bluf +25.6 / event_date +19.8 / pmesii +12.8 等) は「gemma が
  空にする任意フィールドを教師が埋める」充足パターン差。正解が無い以上「改善」とは
  呼べない。importance 一致 90%・intent 一致 77% — **不一致箇所の正誤は人間裁定
  サンプルでのみ確定可能**
- ⚠ 測定の限界: ①rationale 充足 95→50% は bridge の structured required 強制の差
  (配管差) の疑い — モデル知能の差と混同しない ②goldset は易例が支配的で、教師優位が
  あるとすれば過少代表の難例側 ③sonnet + 現行プロンプトは教師の上限ではない

**含意**: 品質目的の教師蒸留の余地は現時点で限定的。生徒モデル (H2) の第一の価値は
**速度・コスト・ループ反復の実践**に置き、蒸留元は 26B + 決定論フィルタで足りる。
品質の実効打は引き続き候補到達率 (§10.2c) と、不一致サンプルの人間裁定による
教師較正が先。

### 10.2e 教師較正 — E1 錨つき母集団での正解率測定 (2026-08-22) → 教師採用は棄却

§10.2d の測定は凍結 goldset (86 件) 上で行ったが、**goldset と E1 ラベル (51 件) は
1 件も重ならない** (実測 0 行) ことが判明した。つまり §10.2d が測れたのは「教師と
gemma の一致率」であって正誤ではなく、不変条件 13 (優越の主張はラベル有り場のみ) の
下では教師採用の根拠にならない。母集団を逆にして測り直した (`scripts/measure_teacher_on_e1.py`)。

- **設計**: E1 ラベルのある 51 記事を母集団に、本番 main 2 回 (揺らぎの床) + 教師
  (claudecode:sonnet) 1 回を盲検再判定し、E1 の錨に対する**正解率**を測る
  (不変条件 5 の 3 者比較)。候補未到達は別掲、few-shot は構造的 off (§13-6)
- **結果 (判定対象 22 件)**: main-a 100% / main-b 100% / 教師 100% — 不一致 0 件
- ⚠⚠ **判定は「測定不能」(2026-08-22 同日、独立レビュー H2 で当初結論を撤回)**:
  判定対象 22 件のうち **candidate_count=1 が 20 件** (実測)。`reachable` の定義上
  母集団は「正解が候補に含まれる」ケースのみなので、20/22 は**唯一の候補を選ぶか
  空にするかの退化課題**だった。全員 100% は課題が易しすぎたことの表明であって
  能力の同値ではない。**「揺らぎの床」も退化課題では構造的に 0 になり、
  `abs(delta) <= floor` の判定式は何も判定していない**
- ⭐⭐ **教訓: 検出力ゼロの検定で「棄却」を決定記録に書くのは、「採用」を書くのと
  同じ誤り**。不変条件 13 (ラベル有り場でのみ主張) は満たしていたが、
  **「ラベルがある」と「識別できる」は別条件**だった → 不変条件 13 に検出力の要件を追記
- 再測定するなら: 母集団を candidate_count ≥ 2 かつ main の 2 回が割れた事例に限定し、
  床は同一プロンプト反復でなく入力摂動 (言い換え・順序) で作る
- ⚠ **「E1 母集団の本番正解率 98%」も撤回** (独立レビュー H3): 51 記事の帰属内訳は
  feed_match 19 / title 37 (fan-out 込) / llm 2 で、feed_match 分は
  `subject_backfill` が `label_harvest` の突合定数をそのまま import している
  **同一規則の出力＝答案の自己採点**。全記事でも主体帰属は title 2,139 / feed 908 /
  llm 564 / feed_match 19 = **84% が決定論**であり、LLM が実際に判断した 564 件に
  E1 ラベルが落ちているのは **2 件**。「98%」は主として決定論経路の自己確認だった
- ⚠⚠ **E1 錨そのものが自己突合** (独立レビュー H1、最重要): E1 ラベル 51 件の記事ホストは
  **www.ransomware.live が 43 件** (実測)。突合の左辺 (犯行声明) も右辺 (「ニュース記事」)
  も同一ソースで、§3 が謳う「未来が過去の判定を裁く」は成立していない。第三者報道による
  真の遅延正解は 90 日で **5〜8 件**。`fetch_victim_org_news_candidates` は
  `subject_actor_source <> 'feed'` しか除外せず**ソース独立性を要求していない**
  → 不変条件 14 (下記) を追加し、独立ソース subset で全数値を再計算する必要がある
- ⭐⭐ **副産物として「候補未到達」の正体が判明**: 未到達 29 件のうち **28 件は候補ゼロ**
  (本文にも表題にも既知アクター名が 1 つも無い = 被害報道だけがあり犯行主体は
  リークサイト側にしか存在しない類型)。**LLM には原理的に到達不能で、正しくは決定論
  経路の仕事**。実際 29 件中 28 件は本番が既に正解を入れていた (feed_match 18 +
  title 10)。**E1 母集団での本番 subject_actor 正解率は 50/51 = 98%**
- **含意 (優先順の更新)**: 「候補到達率が最大の gap」という §10.2c 時点の見立ては、
  ロードマップ A (feed_match 転移帰属) の投入で**測定可能な母集団については解消済み**。
  残る 1 件は llm 由来の誤り。⚠ ただし E1 母集団は「犯行声明と突合できた事例」に偏った
  スライスであり、全記事母集団を代表しない (パネル母集団での未到達 26% とは別の数字)

### 10.2f 一貫性検出 (C1-③) のオフライン実測 (2026-08-22) — 実験終了条件の判定材料

§10.3「実験の終了条件」の判定実測。embedding クラスタ (時間 14 日窓 + コサイン閾値、
union-find) で「同一事象・複数記事」を定義し、クラスタ内の判定分裂率を測った
(`scripts/measure_cluster_consistency.py`、24,795 記事、閾値 3 水準スイープ)。

| t | クラスタ(≥2) | 被覆記事 | intent 分裂 (全体/単一host/複数host) | stance | importance |
|---|---|---|---|---|---|
| 0.80 | 1,105 | 2,988 | 23.2% / 27.9% / 21.3% | 22.8% | 23.9% |
| 0.85 | 552 | 1,366 | 22.2% / 32.8% / 17.6% | 19.3% | 19.2% |
| 0.90 | 223 | 541 | 32.0% / 38.9% / 25.6% | 16.2% | 15.9% |

- ⭐⭐ **決定的な発見: 単一ホスト・同一事象の連報クラスタで intent が割れる**
  (例: 同一メディアがほぼ同文でテンプレ連報した単一事件 12 本で disruption/influence/None
  が混在)。ソース間の正当な視点差では説明できない **純粋な分類器不安定性**の、
  誤検出の少ない証拠。正解ゼロの intent に初めて機械的な誤り信号が立った
- **ただし錨 (E1 級ラベル源) にはならない**: 検出できるのは「少なくとも一方が誤り」
  までで、どちらが正しいかは言えない。stance の分裂は媒体ジャンル差 (news brief vs
  deep-dive) という正当な多様性を多分に含む。大規模クラスタ (サミット報道 44 本) は
  進行中 storyline の副事象差を含み分裂率を過大評価する
- **判定: 「粗い監査信号 (主に intent、単一ホスト subset が最良)」としては成立、
  「ラベル源」としては不成立**。人間裁定キューに流すには分裂が多すぎる (数百クラスタ) —
  正しい消費形態は**キューでなく指標** (週次の intent 不安定率 = rubric 変更の
  ドリフト番兵、決定論・LLM 呼出ゼロ)
- ⚠ ホスト多様性の層別は不完全 (rt.com と sputnikglobe.com は編集上同一系列だが
  別ホスト扱い)。将来使うなら媒体系列辞書が要る

### 10.3 決定記録

| 日付 | 事項 | 決定 |
|---|---|---|
| 2026-08-21 | §8-1 外部 LLM のパネル利用 | **条件付き承認** — エスカレーション量の実測 (10.2) 後に予算・接続を最終判断 |
| 2026-08-21 | 本番接続の方式 | **シャドー先行を全 Phase に義務化** (10.1) |
| 2026-08-21 | §8-2 裁定 UI の置き場 | **運用レビュー (Intel Graph operations タブ) に第 3 サブビューとして統合**。置き場は既存タブに乗せて導線・fullOnly/denylist 境界・承認=適用の終端保証を再利用し、ビューの中身は C5 要件 (証拠提示・2 択化・バッチ承認抑止) の専用設計とする。rubric 提案 (C6) の承認は prompt blocks の版付き編集への導線で終端を保証する |
| 2026-08-21 | §8-3 P1 遅延正解ソースの優先順 | **P1 は ①feed 突合 → ②MITRE 確定 の 2 本のみ実装**。③dedup クラスタ不整合はラベルでなく係争起票のため P3 (パネル入力) へ移管、④PIR 事後マッチ乖離はシャドー集計のみで観察してから採否を決める |
| 2026-08-21 | 既存運用レビューの格子統合 (§11) | **承認** — E3 producer 合流を P1 に併設、証拠リンクは即時の小改修、人間裁定の事後監査と区分1 自動化判定は P3、裁定 UX 統一は P4 |
| 2026-08-22 | §8-2 裁定 UI の実装形 (追記) | P4 実装は**運用タブの自己隠蔽カード** (待ち 0 なら非表示) — 着弾 月 1-2 件の実測 (§10.2b) を受けた最小形。第 3 サブビューへの昇格は着弾が増えたら |
| 2026-08-22 | ROI ゲート (§13-15 の提案を採用) | **8 週 (〜2026-10-17) で cutover した格子コンポーネントがゼロなら、格子への新規投資を凍結し候補到達率 (決定論改修) へ全振りする**。判定材料はシャドー台帳 (§13-8) の週次起票 |
| 2026-08-22 | §13 台帳の即応対処 | #2 TTL 非破壊化 / #3 snapshot 凍結 + 日次資産エクスポート / #6 測定経路の構造的 off + プール確認済み限定 / #8 シャドー台帳 + 期限起票 / #1 部分 (rubric 版 stamp + generic org ガード) / #17 rubric 版記録 — を実装 (残 = 各台帳エントリ参照) |
| 2026-08-22 | **C7 の cutover (シャドー解除)** | `TUNING_AUTO_ROLLBACK=1` を本番適用。判断根拠は実測でなく設計論拠 — C7 の作動条件 (verify FAIL) は稀でシャドーデータは構造的に溜まらない / fail-closed 方向のみ / ガード 4 種はテスト済み / 影響半径は版履歴付き config 1 版の巻き戻しで人間が即復元可能。**格子初の cutover = ROI ゲート (§10.3) の条件を充足** |
| 2026-08-22 | 主体の転移帰属 (feed_match) 導入 (§13 対処 A) | 犯行声明 (E0) × victim_org ±5 日突合で、主体が空の記事へ `subject_actor_source='feed_match'` を決定論補完する。**候補ゲート (本文言及集合) の例外だが LLM 推測ではない** — 証拠等級 feed > feed_match > title > llm。既存主体は上書きしない・競合はスキップ (保守側)。候補未到達 26% (§10.2c) への LLM 迂回の実効打 |
| 2026-08-22 | **本設計の位置づけを「実験」と明文化 + 終了条件 (利用者決定)** | 利用者の言葉: 「LLM 精度向上などを目的とした実験的性質が大きい。現状に悪影響を及ぼすことを望まない」。これを充足条件に翻訳する — ①**実験の最後の仮説 = C1-③ 一貫性検出** (embedding クラスタ内の判定不整合。正解ゼロの intent/stance に届く唯一の機械的信号)。オフライン実測で「監査に使える信号」と確認できなければ**格子を縮退する**: パネル (C2)・裁定キュー (C5)・few-shot (C8)・supply_sentinel を退役し、goldset / verify / fill-rate 監査 / asset_export (いずれも格子以前からの資産) と収穫済みラベルのみ残す ②**現状への悪影響の遮断**が縮退より優先 — C7 破局ゲート化 (H6) と不変条件 14 (自己突合排除) は縮退の如何に依らず即時実施 |
| 2026-08-22 | **ML の位置づけ (利用者意図の明文化)** | ML は LLM の正しさを証明する装置ではない。**集合レベルの判定 (バースト / クラスタ / 異常検知 — per-article LLM には原理的に不可能でラベル不要) を検出し、その結果を LLM が生成する状況分析 (synthesis) や朝ブリーフへ反映する**のが正しい配線 (実装済みの例: 急増検知 → 朝ブリーフ節)。LLM→構造化→ML→LLM 文章化 の循環であり、ML の役割は「LLM が見えないパターンの検出」、LLM の役割は「文脈化と叙述」 |
| 2026-08-22 | **C7 を破局ゲートに限定** (独立レビュー第 2 回 H6) | 無人作動条件を 分布シフト >20pt / 全滅級低下 >15pt に縮退。充足率小幅ゲート・要約長ゲートは人間向け報告のみ (片側 Goodhart ラチェットとブレビティ自動巻き戻しの除去)。TUNING_AUTO_ROLLBACK=1 は維持 — 無人域を狭めたのであって安全網を捨てたのではない |
| 2026-08-22 | ~~教師を報酬源に採用しない~~ → **測定不能 (検出力不足) に格下げ** (§10.2e、同日の独立レビュー H2 で撤回) | 当初「E1 母集団で 100%/100%/100%、不一致 0 ゆえ棄却」と記録したが、**判定対象 22 件のうち 20 件が candidate_count=1** (実測) = 「唯一の候補を選ぶか空にするか」の退化課題であり、検出力がゼロだった。**棄却も採用も主張できない**。few-shot 棄却 (床付き 3 者比較で干渉まで検出) と同列に扱ってはならない。再測定するなら母集団を candidate_count ≥ 2 かつ main が割れる事例に限定する |
| 2026-08-21 | 人間関与の設計原則 (§12) | **人間は加速材であって燃料ではない** — 全タッチポイントに無応答既定 (保守側) + TTL を義務化。事前承認は前向き rubric 適用のみに限定し、他は事後 veto に転換。人間の非介在でループは減速するが停止しない |

## 11. 既存運用レビューの格子への統合 (2026-08-21 承認)

既存の運用レビュー (Intel Graph operations タブ: taxonomy / editorial、および actor 承認
キュー) の精度問題 3 点 — ①証拠が件数のみで記事に到達できない ②editorial 訂正が
write-only (消費者ゼロ) ③人間裁定が較正されない — を、較正格子の部品で解消する。
統一する動きは 1 つ: **既存レビューの人間裁定をすべて tuning_labels (E3 層) に合流させる**。

| 施策 | 内容 | 乗せる先 |
|---|---|---|
| A. E3 ラベル合流 | taxonomy 採否・editorial 訂正を tuning_labels に書く producer を追加 (過去分 backfill 可)。editorial の write-only が解消し、訂正が C8 few-shot 経由で分類精度に還流 | P1 に併設 |
| B. 証拠リンク | taxonomy 提案の evidence_ids (保存済み・未表示) を ArticlePeek で開けるリンクにする | 独立・即時 |
| C. 人間裁定の事後監査 | E1 到着時に裁定を突合し override 成績を可視化 (C5 の「人間も較正される」の遡及適用)。区分1 の人間同意率を実測し、十分高ければ C7 型 (自動適用 + rollback + ops 通知) へ昇格 | P3 |
| D. 裁定 UX 統一 | P4 の裁定サブビューの「1 件 = 証拠 + 2 択」カード部品を taxonomy にも適用 | P4 |

ガード: E3 ラベルは E0/E1 より弱い強度として層別 (C3)。few-shot 投入時は rubric 版数
紐づけ (C8) を適用。

## 12. 人間非介在の縮退設計 (2026-08-21 承認)

**原則: 人間は加速材であって燃料ではない。** ループの進行 (計測・ラベル生産・few-shot
更新・自動 rollback) は E0/E1 の決定論ソースだけで自走する。人間裁定 (E3) は精度を
上げる追加供給であり、欠けてもループは減速するだけで停止しない。これを保証するため、
**すべての人間タッチポイントに「無応答時の既定挙動 (no-human default)」を定義する**。
既定は常に保守側 (現状維持 / 不採用 / 除外) で、キューは無限成長しない (§5 不変条件 9)。

### 12.1 タッチポイント別の無応答既定

| タッチポイント | 人間がいる場合 | 無応答時の既定 | ループへの影響 |
|---|---|---|---|
| C5 係争裁定 (パネル分裂) | 2 択裁定 → E3 ラベル | TTL 30 日で「未裁定」として自動アーカイブ (ラベル化せず few-shot 不投入)。**E1 が後日到着したら人間を追い越して自動確定** | 該当サンプルが学習材から欠けるだけ |
| C6 rubric 提案承認 | 承認 → 版付き適用 | 未承認のまま現行 rubric で運転継続 (現状維持は安全な定常状態)。同一 block への新提案で旧提案は superseded 自動失効 | 改善が遅れるだけ。測定・提案生成は自走継続 |
| C7 自動 rollback | — | **人間不要 (唯一の無人適用)** — 劣化防御は人間不在でも作動 | なし |
| taxonomy 提案 | 採用/却下/保留 | pending のまま週次 upsert で evidence refresh、TTL で stale 自動 expire。区分1 は同意率実測後に C7 型自動適用へ昇格 (§11-C) | 辞書更新が遅れるだけ |
| editorial 訂正 | 訂正 → E3 ラベル | 任意入力。訂正ゼロでも stance の較正は E1 (続報整合) で継続 | なし |
| ラベル自動採用 (C4 高確信帯) | **事後 veto** (来歴付き取り消し → few-shot から除去) | veto なし = 採用のまま | なし |

### 12.2 事前承認から事後拒否権への転換

- **ラベル採用は事前承認不要の自動** (C4 高確信帯)。人間は事後に veto できる。誤りの
  帰結が回復可能 (ラベル 1 件の混入は局所的で複利しない) なため、「auto-apply の誤りは
  複利」の原則 (§1) に抵触しない。veto は来歴に残し、veto 率は C3 のドリフト番兵に入れる。
- **前向き rubric 適用だけは人承認を維持** (帰結が複利するため — 不変条件 3)。ただし
  これは「承認がないと止まる」ではなく「承認がないと現状維持」であり、現状維持は常に
  安全な定常状態。ブロッカーではない。

### 12.3 キュー衛生の可観測性

- 各キューの滞留・自動アーカイブ率・TTL expire 件数を fill-rate 週次監査に登録し、
  閾値超過は ops 通知 (DB 永続 — 人間が見なくても記録は残る。「成功も記録する」原則)。
- 人間の非介在そのものは異常ではない (設計上の想定状態)。異常として扱うのは
  「自動化率が想定帯から外れる」ドリフトのみ。

## 13. 設計再評価の見落とし台帳 (2026-08-22、自己点検 + 独立敵対レビューの統合)

P1〜P5a 実装後の再評価。自己点検に加えて独立レビューアによる敵対的レビューを実施した
結果の統合 (独立レビューは自己点検が見逃した構造的欠陥を複数発見 — 「自己評価の盲点は
相関する」の実演)。**未対処のまま忘れないための台帳** (対処したら日付を書いて消し込む)。

### HIGH — 構造的欠陥 (対処必須)

| # | 見落とし | 内容 | 対処案 |
|---|---|---|---|
| 1 | **E1 の閉ループ性** (独立レビュー) | 突合キーのニュース側 victim_org は **summarizer の LLM 出力** (実装確認済み) — 「決定論の錨」の供給母集団が rubric 版に依存する。最悪形は静かな縮退 (モデルがあるクラスの victim_org を書かなくなるとその層のラベルが消え、縮小したバイアス標本の上で「改善」を測り続ける)。一般語組織名のガードも無い | ラベル行に rubric 版 + victim_org 由来を記録し、供給量の版間ドリフトを番兵登録。組織名に generic denylist / 最小トークン数ガード |
| 2 | **TTL 失効が split 込みで E1 を破壊** (独立) | 稼働中のコード。expire は `split` (パネルが割れただけ) も含めて E1 を隔離 = **パネル合意が錨を消す** (§5-1/2 の実装違反)。人間不在はループを減速させるだけでなく資産を侵食する (§12 違反) | TTL 既定を非破壊 (`disputed` フラグで学習除外、ラベルは残す) に変更。隔離対象を `unanimous_wrong` のみに限定 |
| 3 | **恒久資産の保全欠如 + 標本の構造天井** (自己 + 独立) | goldset/eval runs は gitignore + pg_dump 対象外の単一ディスク (実機確認済)。ラベルは本文を写さず本文は 90 日 purge → **使用可能標本は常時 ~130 件が天井で何年運転しても増えない** (LIMA/KTO の前提と正面衝突) | 収穫時に (title/本文抜粋/候補/答え/来歴) をラベル行へスナップショット。goldset+labels を schema 非依存 JSONL で週次エクスポートし別媒体退避 |
| 4 | **充足率 Goodhart** (独立) | 自動品質ゲート (goldset compare / cutover verify / C7) は実質すべて充足率。C6 自動提案は「より多く書かせる」方向へ単調に進み、それが自動評価で PASS し続ける。壁 3 はパネル合意しか守っていない | 不変条件に「**充足率の上昇単独を改善と判定しない**」を追加。C6 提案は (a) 出力長・required 数の非増加 (b) E1 突合フィールドの正解率、の同時充足のみキューへ |
| 5 | **C6 の多重比較** (独立) | 固定 86 件 × ~20 フィールド × 床 ±4pt に自動で変種を投げ続ければ偶然の「床超え改善」が必ず出る。人間承認には統計的に選別済みの見た目に強い証拠が届くため veto が機能しにくい | goldset を dev/holdout に分割し、dev 通過候補のみ holdout で確認。提案に「今週この block で試した変種数」を必須表示 |
| 6 | **C8 有効化が C2/goldset の独立性を同時破壊** (自己の弱い版を独立が強化) | few-shot はパネル・goldset も通る同一経路に注入され、パネルは自分が監査すべき答えを教材として読む (§10.2b の監査機能が消える)。自己混入ガード 0.97 は同一事件の別媒体記事 (典型 0.85-0.95 帯) を通す。候補未到達例は「候補外の答え」を教える構造ゲート破りの教材 | パネル・goldset 経路は few-shot を**引数で構造的に off** (フラグに依らず)。プールは unanimous_correct 確認済みラベル限定 + 同一 victim_org/claim クラスタ排除 + 到達可能例フィルタ |
| 7 | **triage 足切りは全ゲートの死角** (自己 + 独立) | 棄却 low は DB に残らず (実機確認済)、goldset・cutover verify・fill-rate 監査は**すべて配信済みしか見えない**。ヘッドが再学習であるクラスを静かに落としても指標は正常のまま — 致命失敗 (稀少 high 取りこぼし) が最も起きる場所が最も観測されない | ヘッドの足切りは shadow 限定 (「LLM と不一致の足切り候補」を記録するのみ)。cutover するなら足切り母集団への固定サンプル再判定 (メインで週 N 件) を同時義務化 |
| 8 | **シャドー終了条件の欠如** (独立) | 実装済み主要 4 部品 (few-shot/auto-rollback/パネル/裁定) が**すべて無期限シャドー**。単独運用では「シャドーのまま忘れる」が既定平衡で、C7 (唯一の無人安全網) は一度も作動しない | 各シャドー部品に cutover 判定の必要標本数 + 期限 (例 8 週) を定義し、期限到達で「cutover/破棄/延長」の 3 択を ops へ強制起票 (§12.3 の対象化) |

### MEDIUM — 整合性・適用範囲・責任分界

| # | 見落とし | 内容 / 対処案 |
|---|---|---|
| 9 | 生徒 cascade の責任分界未定義 (独立) | 「生徒が壊れても本番は劣化しない」は cascade 図 (高確信は直接採用) と矛盾。→ 「生徒の失敗はメインへ fail-open」と改め、`STUDENT_CASCADE=0` 型 rollback フラグ・生徒不在時全件メインを実装前に固定 |
| 10 | §10.2b の結論が最易スライスからの外挿 (独立) | パネル母集団は「候補到達済みの多肢選択」のみ (未到達 38/63 除外)。分裂率 0% から外部 LLM 予算・裁定 UI 最小を導くのは過大。両文書が同一データを逆に解釈する矛盾も。→ §10.2b に母集団限定を明記、分裂率は 90 日窓 + 層別で再掲 |
| 11 | C7 の適用が 15 本中 1 本 (独立) | 非対称性論法 (後ろ向きは無人) が summarizer 以外で成立していない。rollback 後の park は §12.1 の「人間不要」と矛盾。→ config_key 引数駆動で judgment/triage へ拡大、park は TTL 付き再通知 + 無応答時は rollback 版継続を既定に |
| 12 | 裁定の到達性 (独立) | 唯一の load-bearing 人間タスクが Tier2 ローカル限定 + キューの存在すら公開面から不可視で、不在時に実行不能 × TTL 破壊 (#2) と併発。→ 閲覧を Tier1 へ昇格 + write は narrow proxy 型、または #2 の非破壊化で片方は解消 |
| 13 | 「ラベル 1 件は複利しない」の前提失効 (独立) | H2 (重み学習) 参入で失効する。事後 veto の実装も無い。→ 「複利しないのは H1 の間のみ」と明示し、H2 参入条件に veto 経路 + ラベル来歴の実装完了を追加 |
| 14 | H2 SSoT の置き場 (独立) | 最も侵襲的な変更 (カスケード/LoRA/RLVR) が規律の緩い調査文書側にある。MLX 学習と Ollama 推論の unified memory 競合・実行帯 (デプロイ禁止帯) も未検討。→ H2 アーキテクチャを本設計書へ移し、不変条件・シャドー基準・rollback フラグ・学習ジョブの実行帯を先に固定 |
| 15 | 文書ドリフト + ROI ゲート不在 (独立) | §3「無条件の錨」vs 実測ノイズ 9% / §4 C2 の Llama 8B vs 実装 gpt-oss / §6 Phase 表の陳腐化 / §8 と §10.3 の重複。実測利得は現状「ラベルノイズ 5/56 検出」1 件で、格子より上流 (候補到達率) に効く手が残っている。→ 文書修正 + 「**8 週で cutover した格子コンポーネントがゼロなら新規投資を凍結し候補到達率へ全振り**」の ROI ゲートを明記 |
| 16 | goldset の鮮度・感度 (自己) | 版付き再構築周期 + 難例スライス (裁定・不一致事例) 併設 (#5 の dev/holdout と併せて設計) |
| 17 | panel_verdicts に rubric 版が非記録 (自己) | 累計分裂率・較正曲線が版横断で regime 混合。→ 裁定行に判定時の版を記録 |
| 18 | §12.3 の監査登録が未実装 (自己) | パネル週次通知のみ。→ 実装するか本文を現実に合わせる |
| 19 | 正解なしフィールドの cutover 基準 (自己) | E1 の無い場はメインとのパリティしか根拠が無い。→ 「パリティ目標・優越主張はラベル有り場のみ」を明記 |
| 20 | 一括教師ラベリングの外部送信手続き (自己) | 数千件送信は事前見積 + 明示承認 + 量 cap を §10.2 に接続して明文化 |

### LOW — 注意書き

- embedding モデル交換でヘッド/few-shot プールの vector が全滅 — 成果物は embedding モデル名に版バインド
- ヘッド成果物 (分類器) の版管理・保存場所が未定義 — rubric 版 + embedding 版バインドとセットで決める
- 教師測定 (§10.2d) の fallback 混入検証手順が未整備 — 次回測定までに bridge 側ログ検証を用意
- バースト検出などの新シグナルは「新しい分析列の 3 点セット規約」の適用対象
- 突合の組織名一致は LOWER(TRIM) 完全一致のみ (#1 と併せて generic ガードを導入)

### 独立レビューの優先 3 点 (そのまま採用)

1. **#2 (TTL がラベルを消す)** — 唯一「今すでに動いていて恒久資産を毀損する」もの
2. **#6 (C8 有効化が測定器を壊す)** — cutover した瞬間に測定器が壊れ、壊れたことが測定器から見えない
3. **#4 + #5 (充足率 Goodhart + 多重比較)** — C6 自動化の前に決めないとループは「測れる指標を上げる」方向へ確実に走る

### 消し込み記録

- **2026-08-22 実装済み**: #2 (TTL 非破壊化 — 不変条件 11 に昇格) / #6 (測定経路の
  構造的 off 引数 + プールを unanimous_correct 確認済み・候補到達可能例に限定) /
  #8 (シャドー台帳 `shadow_registry` + 週次ガバナンスでの期限起票、ROI ゲートの判定材料) /
  #17 (panel_verdicts に rubric_version 記録)
- **2026-08-22 実装済み (第 2 陣)**: #1 残 (E1 供給ドリフト番兵 `supply_sentinel` —
  週次収穫通知に供給状況 + 崩落警告を常設) / #8 の初回 cutover (C7 実適用 —
  シャドー台帳から自動消灯、ROI ゲート条件充足)
- **2026-08-22 部分対処**: #1 (ラベル来歴に summarizer rubric 版を stamp + generic 組織名
  denylist + ドリフト番兵 — **版別層別は版が動いてから**) / #3 (snapshot 凍結 (収穫時 +
  backfill) + 日次資産エクスポート `asset_export` → data/backups、**外部媒体退避は LOW
  のまま**) / #4 (不変条件 10 追加。**C6 実装時のゲート適用は P5b の前提条件**) /
  #12 (#2 の非破壊化で TTL 併発リスクは解消。**Tier1 閲覧昇格は未着手**) / #15 (§3 錨の
  注記・§4 C2 実装追記・ROI ゲート採用。**§6/§8 の全面改稿は未実施**)
- **2026-08-22 実装済み (第 3 陣、ロードマップ C 着手時)**: #7 (不変条件 12 に昇格 —
  ヘッド v0 は shadow 限定で「LLM と不一致の足切り候補」を head_shadow に永続化、
  cutover 時の固定サンプル再判定義務を §14 に明記) / #19 (不変条件 13 に昇格) /
  #14 部分 (ヘッド v0 のアーキテクチャ SSoT を §14 として本設計書に固定。**生徒
  (LoRA/KTO/RLVR) の SSoT 移設と unified memory 競合・実行帯の検討は生徒着手時**)
- **未着手**: #5 (dev/holdout — P5b 前提条件) /
  #9 (生徒実装時の前提条件) / #10 / #11 / #13 / #14 残 / #16 / #18 / #20

### 13b. 第 2 回独立敵対レビュー (2026-08-22 夕 — 実測群を受けた再レビュー)

第 1 回 (§13) と同じく独立レビューアが自己点検を大幅に上回った。番号は 2R- 接頭。

| # | 指摘 | 要点 / 対処 |
|---|---|---|
| 2R-H1 | **E1 錨の 84% が ransomware.live 自己突合** | 全測定結論の前提を汚染。→ 不変条件 14 新設 + 収穫に host 独立性ゲート + 既存自己突合分を strength='self_source' で非破壊隔離 + 番兵の分母層別 (実装済み同日) |
| 2R-H2 | **教師棄却は検出力ゼロ** (判定 22 件中 20 件が候補 1 択の退化課題) | → 「測定不能」に格下げ済み (6bf099d)。不変条件 13 に検出力要件を追記 |
| 2R-H3 | **「本番 98%」は 84% 決定論の自己確認** (LLM 判断分の錨は 2 件) | → 撤回済み。正解率は必ず production source で層別報告 |
| 2R-H4 | **実効被覆率は 1.4% でなく 0.35%** (分母は LLM が判断した件数)。LF#1 増強では原理的に上がらない | → 被覆率定義を「ラベル数 / LLM 判断件数」に変更。錨を打てる領域と使命の重要領域は構造的に反相関 — 「錨を増やす」戦略自体への反論として記録 |
| 2R-H5 | **goldset と E1 は交わらない 2 つの計測系** + 「重なり 0 行」の当初測定は join キー不整合 (goldset は整数 id、labels は URL 文字列) の産物 | → goldset の id 名前空間統一は縮退判断後に。真の重なりも 0 (再計算済) なので §10.2e の結論は生存 |
| 2R-H6 | **C7 が片側 Goodhart ラチェット + ブレビティ自動巻き戻し** (稼働中の実害候補) | → **破局ゲート化 実装済み (同日)**。不変条件 2/10 の実装違反を解消 |
| 2R-H7 | **ROI ゲートが起票当日に「実測でなく設計論拠」の cutover で自己充足** | → ゲートは §10.3 の実験終了条件 (一貫性検出の実測判定) に上書きされた。教訓: ゲート条件は「cutover 数」でなく「測定された利得の出荷数」で書く |
| 2R-H8 | **snapshot 列が write-only** — panel/few-shot の取得口は articles.body を読み続け、本文 90 日 purge で標本天井 (~130 件) は未解消 | 未対処 — 縮退判断後に (縮退なら消費者ごと退役) |
| 2R-H9 | **「パネルを畳む」も同じ退化母集団に基づく早計** — 分裂率 0% は文献上「相関の証拠」でもあり、1 択課題では何も言えない | → 畳む判断は実験終了条件 (§10.3) に統合。継続する場合は母集団を独立ソース + 候補 ≥2 に限定 |
| 2R-H10 | **ヘッドの ECE 0.028 は集合値** — high recall 0.498 で使命の致命方向が未測定。棄却側 (配備母集団) には教師ラベルが原理的に無く §14.4 基準 1 は棄却側で計算不能 (selection bias) | 未対処 — §14.4 の cutover 基準は「棄却母集団への層化サンプル正解付与」を前提に要書き直し。8 週観察はそのまま |
| 2R-M 群 | subject_backfill の無人前向き書込 (不変条件 3 の文言違反 → 「決定論的事実転記は例外」の明文化要) / feed_match が自己の再突合対象に残る / goldset_cron が不変条件 10 未実装 / panel_verdicts.rubric_version 全行 NULL / head_shadow PK が再学習後の再記録を塞ぐ / shadow_registry に C2・C5 が未登録 / 「計測器」の大半 (goldset/verify/fill-rate) は格子以前からの資産で格子の功績ではない | 縮退判断とセットで処置 (縮退なら大半が退役対象) |

## 14. H2 第一段: 古典 ML 蒸留ヘッド v0 (ロードマップ C、2026-08-22 着手)

文献調査 (`docs/self_improvement_literature_survey.md`) の優先順 ② の実装。カスケード
運用の型を最小リスクで確立し、能動学習の前提 (較正確率) を得る。**アーキテクチャの
SSoT はこの節** (§13-14 の教訓: 侵襲的変更の SSoT を規律の緩い調査文書に置かない)。

### 14.1 何を蒸留するか

- **入力**: 記事 embedding (article_embeddings.vector、snowflake-arctic-embed2 / 1024 次元)。
  パイプライン上、embedding は意味的 dedup (Phase 1.7) で triage (Phase 1.8) より先に
  全候補記事に対して計算済み — ヘッドのシャドー推論は追加 LLM 呼出ゼロ・追加
  embedding 計算ゼロで triage 地点に挿せる
- **教師ラベル**: articles.importance / articles.category (= **summarizer の最終判定**、
  triage の pre-filter 判定ではない)。学習可能標本は 2026-08-22 時点で **27,453 件**
  (high 4,891 / medium 14,431 / low 8,131 — embedding との join 成立分)
- **既知の母集団バイアス** (§13-7 と表裏): 教師ラベルは triage 通過分にしか存在しない。
  ヘッドが観測する本番入力には triage 棄却層が含まれる — この分布差こそ shadow 記録で
  実測すべきもの (disagree_cutoff)

### 14.2 構成と成果物

- **分類器**: scikit-learn LogisticRegression (multinomial)。class_weight は使わない —
  base rate を反映した較正確率がこのヘッドの価値 (能動学習の不確実性サンプリングの前提)。
  評価は時系列 split (古 80% 学習 / 新 20% 評価、シャッフル禁止) + ECE
- **成果物**: `data/models/head/current/` (model.joblib + metadata.json)。**embedding
  モデル名 + 次元に版バインド** (§13 LOW の消化) — 読込時に不一致なら拒否して fail-open。
  metadata に rubric 版・学習日・標本数・全評価指標を記録。再学習は秒単位 (成果物の
  バックアップは不要 — 学習データ (DB) が資産で、それは既存の asset_export が守る)
- **学習**: `scripts/train_head.py` (手動、v0 は cron 化しない)。週次 cron 化は
  shadow 観察で agreement が安定してから判断

### 14.3 シャドー配線 (不変条件 12 準拠)

- triage 直後に全候補 (通過 + 棄却) をヘッドで予測し、`head_shadow` テーブルに記録:
  ヘッド予測 (importance + 較正確率 + category) × triage LLM 判定 × 通過/足切り。
  **本番の配信判断には一切影響しない** (fail-open: 成果物不在・embedding 版不一致・
  例外はすべて黙って skip + log)
- **disagree_cutoff**: 足切り境界の不一致 (ヘッドなら残す記事を LLM が切った / 逆) を
  フラグ化 — §13-7 の死角に対する初の観測点
- flag: `HEAD_SHADOW` (既定 1 = シャドー記録 on。0 で完全停止)。将来の cascade 実適用
  flag は `HEAD_CASCADE` (未実装、shadow_registry の cutover 判定名として予約)
- shadow_registry に 8 週期限で登録 (§13-8)。週次ガバナンス通知に直近 7 日の
  agreement / disagree_cutoff 統計を常設

### 14.4 cutover 基準 (前もって固定 — §13-8 の教訓)

以下 3 条件の同時充足で「triage 前段の易例スキップ」(高確信 low の足切り、または
高確信 high の triage スキップ) を提案できる。提案は人承認 (不変条件 3):

1. shadow 8 週で importance 一致率が安定 (週次変動 ±5pt 以内) かつ対 summarizer
   一致率 ≥ triage LLM の対 summarizer 一致率 (= ヘッドが triage の代替として非劣)
2. 致命方向 (summarizer high をヘッドが low 判定) が確信度閾値で 0 に制御できることを
   shadow データで実証
3. **足切り母集団への固定サンプル再判定 (メイン LLM 週 N 件) を同時実装** (不変条件 12)

E1 正解 (主体突合) はこのヘッドの場に無いため、主張はパリティまで (不変条件 13)。
