"""同一入力比較による評価基盤 (2026-08-17)。

既存の `verify_prompt_cutover.py` は**期間比較** — 切替前後の同じ幅の窓を比べる。
これはプロンプト変更の合否には使えるが、**モデル/プロンプトの変化と収集コーパスの
変化を分離できない** (窓が違えば入力記事も違う)。

ここは入力を凍結して「同じ記事を新旧に通す」器を提供する。rubric を触る判断を
デプロイ前に、数分で下せるようにするのが目的。
"""
